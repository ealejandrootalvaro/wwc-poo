{
  "name": "nubank",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "test": "jest --coverage --verbose --detectOpenHandles",
    "test:w": "jest --watchAll",
    "build": "tsc",
    "build:w": "tsc -w",
    "start": "npm run build && node dist/main.js < operations.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ts-jest": "^26.5.5"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "@types/node": "^15.0.1",
    "jest": "^26.6.3",
    "typescript": "^4.2.4"
  }
}
