export const FRECUENCY_SMALL_INTERVAL = 3;
/**
 * Interval in milliseconds where there can be no more than ${FRECUENCY_SMALL_INTERVAL} transactions or doubled transaction.
 */
export const TIME_SMALL_INTERVAL = 120000;